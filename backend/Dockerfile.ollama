FROM ollama/ollama:latest

# Start the server in the background, pull the model, then shut down.
# The model weights get baked into this image layer (~5GB).
RUN /bin/ollama serve & \
    sleep 5 && \
    ollama pull qwen2.5vl:7b && \
    pkill ollama || true
