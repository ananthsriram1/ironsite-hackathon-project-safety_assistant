services:
  ml:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app              # live reload on code changes
      - db_data:/app/data   # SQLite file persists between restarts
      - ./models:/app/models:ro  # model weights (read-only)

volumes:
  db_data:
